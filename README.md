# alpine-go-node
Docker for Alpine go node

# Tools

- git
- make
- docker
- curl
- python

# Node and go:

```
$docker run brio/alpine-go-node node -v
 -> v8.2.1
 ```
 
 ```
 $ docker run brio/alpine-go-node go version
 -> go version go1.8.3 linux/amd64
```
